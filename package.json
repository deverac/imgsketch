{
  "devDependencies": {
    "geckodriver": "^3.0.2",
    "jasmine-browser-runner": "^1.1.0",
    "jasmine-core": "^4.4.0",
    "terser": "^5.15.0"
  },
  "scripts": {
    "minjs": "./node_modules/.bin/terser --compress --mangle --output imgsketch-min.js -- imgsketch.js",
    "test": "jasmine-browser-runner runSpecs",
    "testserver": "echo ' ====> Open browser to http://localhost:8888' && echo '' && npx jasmine-browser-runner serve"
  }
}
